<div class="input-field">
    <input type="text" name="nome" value="{{isset($registro->nome) ? $registro->nome : ''}}">
    <label>Nome</label>
</div>

<div class="input-field">
    <input type="text" name="fone" value="{{isset($registro->fone) ? $registro->fone : ''}}">
    <label>Fone</label>
</div>

<div class="input-field">
    <input type="text" name="cpf" value="{{isset($registro->cpf) ? $registro->cpf : ''}}">
    <label>CPF</label>
</div>

<div class="input-field">
    <input type="text" name="estado" value="{{isset($registro->estado) ? $registro->estado : ''}}">
    <label>Estado</label>
</div>

<div class="input-field">
    <input type="text" name="cidade" value="{{isset($registro->cidade) ? $registro->cidade : ''}}">
    <label>Cidade</label>
</div>

<div class="input-field">
    <input type="text" name="bairro" value="{{isset($registro->bairro) ? $registro->bairro : ''}}">
    <label>Bairro</label>
</div>

<div class="input-field">
    <input type="text" name="rua" value="{{isset($registro->rua) ? $registro->rua : ''}}">
    <label>Rua</label>
</div>

<div class="input-field">
    <input type="text" name="numero" value="{{isset($registro->numero) ? $registro->numero : ''}}">
    <label>Número</label>
</div>

<div class="input-field">
    <input type="text" name="cep" value="{{isset($registro->cep) ? $registro->cep : ''}}">
    <label>CEP</label>
</div>

<div class="input-field">
    <input type="text" name="cnh" value="{{isset($registro->cnh) ? $registro->cnh : ''}}">
    <label>CNH</label>
</div>
<br><br>