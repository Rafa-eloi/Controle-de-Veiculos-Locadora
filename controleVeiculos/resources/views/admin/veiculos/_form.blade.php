<div class="input-field">
    <input type="text" name="marca" value="{{isset($registro->marca) ? $registro->marca : ''}}">
    <label>Marca</label>
</div>

<div class="input-field">
    <input type="text" name="modelo" value="{{isset($registro->modelo) ? $registro->modelo : ''}}">
    <label>Modelo</label>
</div>

<div class="input-field">
    <input type="text" name="placa" value="{{isset($registro->placa) ? $registro->placa : ''}}">
    <label>Placa</label>
</div>

<div class="input-field">
    <input type="text" name="estado" value="{{isset($registro->estado) ? $registro->estado : ''}}">
    <label>Estado</label>
</div>

<div class="input-field">
    <input type="text" name="ano_fabricacao" value="{{isset($registro->ano_fabricacao) ? $registro->ano_fabricacao : ''}}">
    <label>Ano de Fabricação</label>
</div>

<div class="input-field">
    <input type="text" name="km_rodado" value="{{isset($registro->km_rodado) ? $registro->km_rodado : ''}}">
    <label>Quilometragem</label>
</div>

<div class="input-field">
    <input type="text" name="crlv" value="{{isset($registro->crlv) ? $registro->crlv : ''}}">
    <label>CRLV</label>
</div>

<div class="input-field">
    <input type="text" name="renavam" value="{{isset($registro->renavam) ? $registro->renavam : ''}}">
    <label>Renavam</label>
</div>

<div class="input-field">
    <input type="text" name="chassi" value="{{isset($registro->chassi) ? $registro->chassi : ''}}">
    <label>Chassi</label>
</div>

<div class="input-field">
<input type="text" name="disponivel" value="{{isset($registro->disponivel) ? $registro->disponivel : ''}}">
    <label>Disponivel para alugar</label>
</div>

<div class="input-field">
    <input type="text" name="valor_diaria" value="{{isset($registro->valor_diaria) ? $registro->valor_diaria : ''}}">
    <label>Valor da diaria</label>
</div>
<br><br>
